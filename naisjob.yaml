apiVersion: nais.io/v1
kind: Naisjob
metadata:
  annotations:
    nais.io/read-only-file-system: "false"
  labels:
    team: nada
  name: quarto-eksempel
  namespace: nada
spec:
  image: "{{ image }}"
  schedule: "0 1 * * *"
